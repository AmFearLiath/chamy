/* Legal Manager — admin sidebar toggle script */
(function () {
    'use strict';

    // Navigation toggle behaviour is provided globally by the theme's `admin.js`.
    // The LegalManager module must not alter menu behaviour or attach its own handlers.
    // This file intentionally does not register click handlers for `.nav-item-parent`.
})();
