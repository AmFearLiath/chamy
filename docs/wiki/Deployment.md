# Deployment

Deployment‑Hinweise und Docker‑Beispiel (Details im `docs/` Ordner).

- Empfohlen: `docker-compose.dev.yml` für lokale Entwicklung mit DB und PHP‑FPM.
- Healthcheck endpoint: `/api/v1/system/health`.
