# Entwicklung

Entwicklungsrichtlinien, Testing und Code‑Style.

- Verwende `phpunit` für Unit‑Tests (`tests/Unit` und `tests/Integration`).
- Nutze PHPStan/Psalm in CI.
- Format: PSR‑12 / PHP‑CS‑Fixer.
